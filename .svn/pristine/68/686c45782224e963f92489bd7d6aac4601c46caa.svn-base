#pragma once

#include "terrain_creator.h"

#include <UnigineObjects.h>
#include <cassert>
#include <string>
#include <vector>
#include <regex>



class terrain_creation
{
public:
    terrain_creation(terrain_creator &terrain_creator);
    ~terrain_creation();

    //update();

    // Delete all landscape layer maps
    void RemoveAllTerrainNodes();

    // recreate terrain all terrain tiles
    //void RegenerateAllTerrain();


    void ExpandTerrainToFitBoundsOfNodes(std::vector<Unigine::NodePtr> nodes_to_expand_to, std::string file_path_to_lmap);

    void ExpandTerrainToFitBoundsOfNode(Unigine::NodePtr node_to_expand_to,
        std::string          file_path_to_lmap,
        Unigine::Math::ivec2 size,
        Unigine::Math::ivec2 resolution);

private:
    terrain_creator &m_terrain_creator;
};

