#pragma once

#include <UnigineObjects.h>
#include <UniginePlugin.h>

class CollapsibleWidget;

class import_ground_height_gui
{
public:
    import_ground_height_gui();

    Unigine::WidgetPtr GetWidget();

    void Show();
    void Hide();

    void UpdateUI(std::vector<Unigine::NodePtr> nodes);
    void SetImportCallback(std::function<void(std::string, bool, float, float)> cb) { m_import_cb  = cb; }

private:
    std::shared_ptr<CollapsibleWidget> m_collapsible_widget;

    std::function<void(std::string, bool, float, float)> m_import_cb;

};

